# べんきょうクエスト（study-rpg）引き継ぎ資料

小5の息子（開成中学校志望・中学受験勉強中）のための、勉強応援ドラクエ風RPG。
このファイルは新規セッションへの引き継ぎ用。まず全体を読んでから作業すること。

---

## 1. このアプリは何か

勉強した時間を経験値に変えてキャラを育てる、ドラクエ風の学習モチベーションアプリ。
本人（息子）が遊ぶ。勉強時間は自己申告なので、アプリ内のタイマーで測る。

### もともとの要望（最初の依頼そのまま）
- 1分勉強すると1経験値、100経験値でレベルアップするドラクエ風RPG
- 勉強時間は自己申告 → タイマー機能があると便利
- 経験値がたまるとお金・武器がもらえる、ボス戦がある
- 見た目もドラクエ風
- セーブ機能あり。前回から何分勉強したかわかると良い

---

## 2. 技術構成・ファイル

- **単一HTMLファイル**: `index.html`（約3,090行）。HTML/CSS/JS を全部1ファイルに内包。ビルド工程なし。
- `audio/` … 教科別BGM・タイトルBGM（mp3）
- `images/` … 勇者・敵キャラ・タイトルロゴ・favicon（png）
- フレームワーク・npm・サーバーなし。**ブラウザで `index.html` を開くだけで動く**。
- 保存は **ブラウザの localStorage**（キー: `study-rpg-v1`）。サーバー保存はしない。
- 主な実機は **iPad の Safari**（PC Chrome でも動く）。iPad前提のバグ修正履歴あり。

### 公開・デプロイ
- GitHub: https://github.com/furukawa-ui/study-rpg
- 公開ページ（GitHub Pages）: https://furukawa-ui.github.io/study-rpg/
- `main` に push すると Pages に反映される。ローカルフォルダは GitHub と接続済み。
- 中身に秘密情報・個人情報は無し（息子が遊ぶだけのゲーム）。Public でも実害なしと判断済み。

> 注: ユーザーの全社ルール（Next.js + Cloudflare等）は「インターネット公開する業務アプリ」向け。
> これは私用の単一HTMLゲームなので対象外。今の構成（単一HTML + GitHub Pages）を維持してよい。

---

## 3. ゲームの中身（現状の仕様）

### 教科（SUBJECTS）
`math 算数 / japanese 国語 / science 理科 / social 社会 / review やり直し` の5つ。
教科ごとに 勇者画像・敵画像・絵文字・BGM を持つ。

### 経験値とレベル
- 1分の勉強 = 1経験値（`review` やり直しだけ **2倍**）
- 100経験値ごとにその教科のレベルが1上がる（`levelOf = 1 + floor(totalExp/100)`）
- レベルアップで `50G ×（上がったレベル数）` のゴールドボーナス＋武器が進化
- 「そうレベル」= 全教科レベルの合計

### ゴールド（G）
- 勉強1分 = 1G（`review` は **2倍**）
- レベルアップボーナス、ボス報酬、トロフィー報酬でも増える
- ショップで使う

### 武器（WEAPONS）
教科ごとにLv 1/3/5/8/12/18/25/32/40/50/60/72/85/100 の **14段階**で名前が進化（例: 算数 ぼうきれ→…→てんくうのつるぎ→…→Lv100「すうりのしんけん」）。演出のみ。

### ボス戦（BOSSES）
- 教科ごとに **11体**。指定レベル到達で挑戦可能（lv 3/6/10/15/25/35/45/60/75/90/100）。Lv100が各教科の最終ボス「○○のしんおう」。
- 4択クイズ（QUIZZES、5年・開成志望レベル想定）に正解でボスにダメージ。HP3〜14（高レベルほど高HP）。
- 倒すと報酬ゴールド（〜3000G、`review` は〜8000Gと高め）。
- クイズは各教科 約23〜24問（受験レベル＋難問を混在）。戦闘時はプールからランダム出題。`review` のボスは算国理社の全問からまとめて出題。
- ショップアイテムが戦闘で使える（ヒント券=4択→3択、ふっかつ薬、いかずちの巻物=即2ダメージ、ちからの薬=HP3→5）。

### 称号・節目（MILESTONES）
- 各教科が **Lv30=達人 / 50=賢者 / 70=覇者 / 100=神** に到達すると称号獲得（例「算数の達人」）。
- 到達時に金ピカのお祝いモーダル（`showMilestone`）＋ゴールド大ボーナス（500/1500/3000/10000G）。
- 獲得済み称号は「アイテムずかん」に一覧表示。
- セーブには `subjects[].milestones[]`（到達したLv番号の配列）で記録。
- **読み込み時 `backfillMilestones()`** で、すでに到達ずみの称号を静かに付与（後出しゴールドは無し）。以降に超えた節目は本番の演出＋ボーナスが出る。

### タイマー（T オブジェクト）
- 教科を選んでスタート → 0.5秒ごとに tick、経過を分換算して経験値付与。
- 一時停止・再開・ストップあり。端数（1分未満）は `bankedSec` で次回に持ち越し。
- **自動一時停止は廃止済み**（意図的。勝手に止めない）。
- **ストップ押し忘れ確認**: 40分ごとに「まだ勉強してる？」モーダル → 5分反応なしで自動ストップ。
- ストップ時に「今回の勉強時間（前回から何分）」を履歴に記録。

### ショップ（SHOP_ITEMS）
3カテゴリ:
- **battle**（戦闘用・使い切り）: ヒント券100G / ふっかつ薬200G / いかずちの巻物400G / ちからの薬160G
- **boost**（時間制ブースト）: けいけんちのお守り600G(30分EXP1.5倍) / ゴールドのお守り400G(30分Gold1.5倍) / 神のいぶき2000G(60分EXP・Gold2倍)
- **collection**（コレクション・飾り）: 名声の盾1000G / 王者の冠6000G(タイトルの名前横に表示) / 古代の竜の鱗10000G
- ブースト中のEXP増分は `bonusExp` に加算（`totalMin` は実勉強時間を保つ設計）。

### トロフィー / 振り返り
- 週・月の勉強時間しきい値で 🥉🥈🥇 トロフィー＋ゴールド報酬。
- `review` やり直しは「毎日」「週」の専用しきい値あり（毎日達成しやすいゴール設計）。
- 月別合計などの振り返り画面あり。

### 画面構成
- タイトル画面（そうレベル表示・BGMトグル・スタート）⇄ ゲーム画面。
- ゲーム画面: キャラ情報 / 教科パネル（2カラム、やり直しは上に独立）/ ボス一覧 / メニュー / ショップ / 振り返り。
- 戦闘シーン演出（勇者↔敵、攻撃エフェクト）。

### BGM
- 教科別BGM＋タイトルBGM。マスターON/OFF・音量（初期5%）・教科別ON/OFF。
- **デフォルトはOFF**（iPadで勝手に鳴らないように意図的）。

---

## 4. 状態（セーブデータ）の形 — `defaultState()`
```
version, hero{name}, gold,
subjects{ 各教科: { totalMin, bankedSec, defeated[], bonusExp, milestones[] } },
history{ lastStudyAt, lastSessionSec },
daily{ 'YYYY-MM-DD': {教科ごとの分} },
trophies[], inventory{}, activeBoosts[],
bgm{ enabled, volume, subjects{} }
```
- 読み込み時 `mergeDefault` で軽いマイグレーション（新キー追加に強い）。
- **既存セーブデータを壊さないこと**。state構造を変える時は `mergeDefault` 経由で互換を保つ。

---

## 5. 作業するときの注意

- **このユーザーは非エンジニア**。専門用語は避け、平易な日本語で説明する。
- **推測でコードを書かない**。仕様の解釈に迷ったら書く前に質問する。
- **頼まれたスコープを守る**。ついでのリファクタを勝手に混ぜない。気になる点は提案にとどめる。
- localStorage のセーブ互換を壊さない（息子の進捗データが消える）。
- iPad Safari で動くことが最優先（過去に Safari 固有のバグ修正多数）。
- commit・push は明示的に頼まれた時だけ。push 前に確認。

## 6. 作成経緯
- 2026年5月5日に旧PC（`C:\Users\FURUKAWA\study-rpg`）で作成。
- 作成時の会話履歴は `C:\Users\furub\Downloads\claude-backup.zip` 内（セッション 1263a1d2 / 642fc6a6）。
  展開済みコピー: `C:\Users\furub\.claude\tmp-old-transcripts\`
- 姉妹アプリ: フラッシュ暗算ゲーム `flash-keisan`（`C:\Users\furub\flash-keisan`、同じ息子向け）。
